階段一：最獨立的底層優化（全域格式化與單位切換）
核心目的：先完成最獨立的功能，改完立刻能在現有畫面上看到成效。

[x] Task 1: 實作全域數值格式化工具函式

在 app.py 頂端新增 format_value(val, unit) 函式，支援千分位逗號，並依據單位（元、萬元、k、M）進行數值除法。

[x] Task 2: 在看板頁面註冊單位選擇器並套用

在 page_dashboard() 頂端新增 st.radio("顯示數值單位", ["元", "萬元", "k", "M"], horizontal=True)。

將現有的 st.metric 指標全數套用 format_value。

🎯 本階段測試點：啟動 Streamlit，確認切換單位時，現有的 KPI 數字會正確換算且網頁沒壞。

階段二：前端 UI 雙欄化與動態表單刻畫（不破壞舊功能）
核心目的：把新的 UI 畫面刻好，但點擊按鈕時先不啟用新欄位，維持舊功能運作。

[x] Task 3: 使用 st.columns(2) 拆分左右大區塊

將設定頁面的自訂手動模擬區塊改為左右雙欄佈局。

[x] Task 4: 建構左欄「💰 收入與投資設定」子頁籤

左欄內嵌 st.tabs(["💵 薪資與年金", "📈 投資資產配置"])。

[x] Task 5: 建構右欄「💸 支出與台灣重大花費」子頁籤

右欄內嵌 st.tabs(["🍔 日常與娛樂", "⚠️ 台灣重大花費設定"])。

在「⚠️ 台灣重大花費設定」中加入 st.multiselect（包含買房、定期換車、育兒、孝親等選項）。

[x] Task 6: 刻畫「🚗 定期換車」與其他動態花費控制項

當選取定期換車時，動態展開：換車頻率（每 N 年）、單台預算、截止年份等自訂欄位。

🎯 本階段測試點：檢查 UI 雙欄排版是否美觀，勾選不同里程碑時，下方的對應自訂表單有沒有正常彈出來。此時點擊「執行模擬」按鈕依然可以跑舊的模擬。

階段三：核心引擎重構與資料封裝（關鍵對接，一氣呵成）
核心目的：將輸入端（UI 欄位封裝）與處理端（計算引擎）同時切換為新架構，消滅斷層。

[x] Task 7: 重構 run_simulation 計算引擎為資料驅動結構

將參數改為接收 milestones_list: list[dict]。

在內部 for 迴圈中，撰寫針對 one_time（一次性）、duration（連續型）、interval（每 N 年定期循環）三種型態的動態支出加總邏輯。

[x] Task 8: 在前端按鈕觸發時進行資料封裝 (Data Packaging)

在 st.button("儲存配置並執行模擬") 被點擊時，動態將階段二做好的 UI 輸入值（如換車頻率、育兒費等）組裝成標準規格的 milestones_list 陣列。

呼叫新版 run_simulation 並將資料與 milestones_list 一併存入 st.session_state。

🎯 本階段測試點：按下按鈕後能成功跳轉到看板頁面，且下方的「歷年詳細模擬數據」表格中的數字完全正確，代表動態計算引擎成功。

階段四：看板與圖表動態化（視覺與里程碑線條串接）
核心目的：將計算出來的動態資料完美呈現在圖表與指標上。

[x] Task 9: 重構 Plotly 圖表中的動態里程碑標記 (add_vline)

淘汰寫死的 house_year 垂直線。改為用一個 for 迴圈遍歷 st.session_state.milestones_list。

若項目為 one_time 或當年度觸發了 interval（換車年），就自動在圖表對應年份加上垂直虛線與標籤（例如：圖表上會自動在 2030、2038、2046 年動態出現「換車」標記）。

[x] Task 10: 更新 Plotly 軸線與 Hover 數字單位

根據階段一選擇的單位，調整 Plotly Hover 提示框與 Y 軸的數值顯示。

🎯 本階段測試點：進入看板，確認圖表上能根據你在前端勾選的項目，動態畫出所有的里程碑時間線，大功告成！